// Spinner.js
import React from 'react';
import '../Spinner.css'; // We'll add some simple CSS for the spinner

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
